# 日本AI原生内容平台(jp)

按照《值得买科技-国际事业部-日本AI原生内容平台 说明书》实现的最小可运行版本(v0)。

**一个日语网站,没有App、没有Feed、没有账号体系。** 两类页面:品牌定位页 + 问答页,面向AI引用做结构化设计。

## 与说明书的对应关系

| 说明书模块 | 本仓库实现 |
|---|---|
| 品牌定位页(§5.1) | `data/brands/*.json` → `/brands/<slug>/`,含母公司、价位带、品类、渠道、日本法人(法人番号)、技適、召回、保修、日语客服9个字段,每条事实带出典与确认日 |
| 问答页(§5.1) | `data/qa/*.json` → `/qa/<slug>/`,标题即问题、首屏即答案、下方事实表格 |
| 页面结构(§5.2) | 无侧边栏、无推荐位、无登录入口 |
| AI引用结构化数据(§5.3) | 品牌页 Article + Brand JSON-LD;问答页 FAQPage JSON-LD;`llms.txt`、`sitemap.xml`、`robots.txt` 自动生成 |
| AI引用监测台(§5.1) | `monitor/` 内部脚本:固定日语query集,每日查询 ChatGPT/Perplexity/Gemini,记录是否被引用及被引用片段 |
| 索引率指标(§8) | 生成 sitemap.xml,可直接提交 GSC |

## 使用方法

### 构建站点

```bash
python3 build.py
```

无任何依赖。读取 `data/` 生成静态站到 `docs/`。已生成的 `docs/` 可直接用 GitHub Pages 发布(Settings → Pages → Deploy from branch → `/docs`)。

### 新增内容

- 新增品牌:在 `data/brands/` 加一个 JSON(参考 `anker.json` 的字段),重新构建。
- 新增问答:在 `data/qa/` 加一个 JSON(`question` / `answer` / `facts`),重新构建。
- 域名:在 `config.json` 里把 `base_url` 改为正式域名(当前为占位 `https://example.jp`)。

### AI引用监测

```bash
export OPENAI_API_KEY=...       # 可选
export PERPLEXITY_API_KEY=...   # 可选
export GEMINI_API_KEY=...       # 可选
python3 monitor/monitor.py
```

按 `monitor/queries.json` 的固定query集逐一提问,结果写入 `monitor/results/YYYY-MM-DD.jsonl`,并输出当日引用率(核心指标:AI引用率)。未设置key的provider自动跳过。上线后把 `queries.json` 中的 `site_domains` 改为正式域名,配合cron每日运行。

## 当前内容

**品牌页 ×16**,每个品牌一个一级 slug(`/anker/`、`/dyson/` …):

| 国 | 品牌 |
|---|---|
| 中国 | Anker、Xiaomi、SwitchBot、Roborock、DJI、Insta360、ECOVACS、UGREEN |
| 日本 | BALMUDA、CIO、エレコム、Nature Remo |
| 美国 | SharkNinja、iRobot、GoPro |
| 英国发祥・新加坡本社 | Dyson |

后6个(iRobot、ECOVACS、エレコム、GoPro、Nature Remo、UGREEN)都是从既有品牌的
「日本での主な競合」字段里挑出来的,所以加进来之后「関連ブランド」的相互链接自动接上了。

每页 **32 个字段、5 个分组**,覆盖说明书§5.1要求并扩充:

| 分组 | 字段 |
|---|---|
| 企業プロフィール | 正式社名、本社所在地、創業年、創業者、上場状況、従業員数、年間売上規模、主な事業領域 |
| 日本での事業体制 | 日本法人、法人番号(13桁)、設立年、所在地、日本参入年、直営店、販売チャネル、日本語公式サイト |
| 規制・安全性 | 技適、PSE(電安法)、リコール履歴、その他の法規制、公的機関からの指摘 |
| 保証・アフターサービス | 保証期間、延長保証、修理体制、日本語サポート窓口、受付時間、消耗品の入手性 |
| 製品ラインと価格帯 | 主力カテゴリ、価格帯、主要製品ライン、日本での主な競合 |

**FAQ**:最早的 10 个品牌各 24 条,新加的 6 个品牌各 10 条,挂在品牌 slug 下
(`/anker/anker-country/`),数据结构化存于品牌 JSON 的 `faq` 数组。

## 现状(2026-08-28)

| 指标 | 值 |
|---|---|
| 品牌 | 16 |
| 事实字段 | 521 项(带出处已核实 366 项 = **70%**) |
| Q&A | 300 条 |
| 时间线 | 146 条 |
| 生成页面 | 320 页 |

**核实率要分开看**:最早的 10 个品牌是 **84%**(276/329),新加的 6 个品牌只有
34〜59%。整体 70% 是这两批平均出来的,不是退步。新品牌未核实的字段在页面上是红字,
「30秒でわかる」框里也写明了「全32項目中 N項目が確認済み」,所以缺口是公开的。

**法人番号**:16 个品牌里 **13 个已确认**,全部来自 gBizINFO(国税庁法人番号公表サイト的
数据),并且逐个通过了国税庁的检查用数字校验。未确认的 3 个是 Insta360(总代理アーキサイト
在登记簿上只有栃木县鹿沼市的同名公司,地址对不上)、iRobot、ECOVACS(登记簿里查不到对应
法人名)——这三个宁可留空也没有猜。

时间线覆盖 2025年12月〜2026年9月。SharkNinja 已从 2 条补到 11 条(2026年4〜8月每月都有)。
仍有缺口:Dyson 的 2026年7月、Roborock 的 2026年8月——两轮独立检索都没找到日期能被
一次来源坐实的日本市场事件,所以留空而不是填推定日期。

## 内容原则

- **FAQ 问题全部来自真实用户提问**,不是想象出来的。来源:Yahoo!知恵袋、Quora日本版、教えて!goo、価格.comクチコミ。每个问题页顶部显示平台名、原帖标题原文和「元の投稿を見る」链接。
- **每条事实标注来源与查询日期**(说明书要求)。
- **未在一次来源核实的内容整行红字显示**,并在页面顶部的「30秒でわかる」框里公示数据完整度(如「全32項目中 21項目が確認済み(未確認 11項目)」)。这样哪些字段还需要人工补齐一目了然。
- 本站只做**品牌层面事实**,不做机型性能实测对比(那是 my-best 等媒体的领域)。

## 设计

遵循日本Web惯例:パンくずリスト、最終確認日、目次、「結論」先行の回答、「30秒でわかる」要点框、よくある質問折叠区、国别徽章(日本=红/海外=藏蓝)、高信息密度带框表格、出典一覧、運営方針页。

## 部署

站点通过 GitHub Pages 发布:https://ptthegreat.github.io/PS-test/
推送代码 → `pages.yml` 工作流自动构建并推送 `gh-pages` 分支 → Pages 自动更新。

⚠️ **样例内容未经日语母语审校,标注【要確認】的事实(法人番号、技適型号、保修期限等)需在一次来源(国税庁/総務省/消費者庁)逐条核实后方可上线。** 按说明书§6.1,所有对外内容需日语母语审校通过。

## 尚未实现(按说明书为后续阶段)

- 联盟转链(M4之后启用)
- 技適/召回的headless批量抓取管线
- 法人番号:13/16 已确认。剩下 3 个需要人工在国税庁网站上按法人名检索确认

## 下一轮的优先事项

1. **补齐新 6 个品牌的核实率**。UGREEN(34%)、ECOVACS(38%)、GoPro(47%)最薄。
   共同的卡点是「特定商取引法に基づく表記」页面的正文取不到——页面确实存在,但检索
   摘要里不出正文。这几个品牌的上市状态、创业年、员工数、技適也都还空着。
2. **新品牌的 FAQ 从 10 条扩到 24 条**,与老品牌看齐。
3. Dyson 2026年7月、Roborock 2026年8月的时间线缺口。
4. Insta360 / iRobot / ECOVACS 的法人番号。
