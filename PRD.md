# ブランドファクト(Brand Facts Japan)产品需求文档

| | |
|---|---|
| 文档版本 | v1.0 |
| 对应代码 | `claude/clear-repo-rename-jp-as139g` @ 16品牌 |
| 编写日期 | 2026-08-28 |
| 线上地址 | https://ptthegreat.github.io/PS-test/ |
| 依据 | 《值得买科技-国际事业部-日本AI原生内容平台 说明书》§5〜§8 |

> 本文描述**当前已实现的产品**,不是构想。所有字段、样式值、校验规则都与 `build.py`
> 和 `tools/` 下的脚本一致。差异之处在 §11 明确列出。

---

## 1. 产品定位

**一句话**:面向日本市场的品牌事实百科,把"这个牌子是哪国的、能不能信、坏了谁修"
这类问题的答案,以**带出处、带确认日**的结构化事实形式沉淀下来,供 AI 引用。

### 1.1 不是什么

明确排除的东西,比排除清单本身更重要——它们决定了页面长什么样:

| 不做 | 原因 |
|---|---|
| App / Feed / 账号体系 | 内容被 AI 引用不需要这些;登录墙会挡住抓取 |
| 机型性能实测对比 | 那是 my-best、家電Watch 的领域,我们做不过也不该做 |
| 侧边栏 / 推荐位 / 广告位 | 稀释正文权重,干扰 AI 提取主体内容 |
| 编辑观点、种草文案 | 与"只登可验证事实"的定位直接冲突 |
| 未标注出处的任何断言 | 见 §7 诚实度原则 |

### 1.2 成功标准

按说明书 §8,核心指标是 **AI引用率**:固定日语 query 集每日提问主流 AI,
统计回答中引用本站的比例。次级指标是 GSC 索引率。
`monitor/` 已实现前者的采集脚本(§9)。

---

## 2. 目标用户与场景

日本消费者在购买决策前的**信任核查**场景,三类高频问题:

1. **来源核查** —「Ankerはどこの国の会社ですか?」
   中国品牌在日本渗透率高,但"哪国公司"「上場しているか」的信息散落各处。
2. **安全与合规** —「技適マークはありますか?」「PSE は?」「リコールは出てる?」
   日本对无线设备(电波法)、电器(电安法)有强制认证,这是本站**独有价值最高**的领域。
3. **售后可持续性** —「保証は何年?」「日本語サポートはある?」「会社が潰れたら?」
   iRobot 破产后这类需求激增,证明该场景真实存在。

这些问题的共同点:**答案是事实,不是观点**;**用户不信任商家自述,需要出处**。

---

## 3. 内容模型

数据源是 `data/brands/<slug>.json`,一个文件 = 一个品牌 = 一个一级 slug。
**没有数据库**,JSON 即真相来源,`build.py` 是纯函数式渲染。

### 3.1 品牌对象

```jsonc
{
  "slug": "anker",              // [a-z0-9-]+,同时是 URL 路径
  "name": "Anker(アンカー)",     // 日文显示名,半角括号内为片假名
  "name_en": "Anker",           // 用于 JSON-LD 的 Brand.name 与关联品牌匹配
  "country": "中国",             // 决定国别徽章颜色
  "summary": "120〜200字",       // 首屏摘要,同时进 meta description 与 llms.txt
  "sample_notice": true,        // 显示"样例内容待校阅"提示条
  "sections": [ /* §3.2 */ ],
  "faq":      [ /* §3.3 */ ],
  "updates":  [ /* §3.4 */ ]
}
```

### 3.2 事实字段:32项 × 5组(**本产品的核心资产**)

每个品牌页固定这 32 个字段。**字段缺失不允许删行**——必须以「未確認」占位并红字显示
(见 §7)。字段名在 `tools/merge_new_brand.py` 的 `CANONICAL` 中定义,合并时严格比对。

#### 企業プロフィール(8)

| 字段 | 说明 | 一次来源 |
|---|---|---|
| 正式社名 | 法人正式名称,与商标名区分 | 公司概要 / 特商法表记 |
| 本社所在地 | 到市区町村;跨国集团标注注册地与发祥地差异 | 公司概要 / SEC filing |
| 創業年 | | 公司概要 |
| 創業者 | | 公司概要 |
| 上場状況 | 交易所 + ticker;非上市也要写明 | IR / 交易所公告 |
| 従業員数 | 必须带基准时点 | IR / 有价证券报告书 |
| 年間売上規模 | 必须带财年(如「2026年3月期」) | IR |
| 主な事業領域 | | 公司概要 |

#### 日本での事業体制(8)

| 字段 | 说明 | 一次来源 |
|---|---|---|
| 日本法人 | 含法人格(株式会社/合同会社)与代表者 | **特定商取引法に基づく表記** |
| 法人番号(13桁) | 国税庁法人番号。**带校验位,可机器验证**(§8.3) | gBizINFO / 国税庁 |
| 日本法人設立年 | | 登记信息 |
| 日本法人所在地 | 特商法地址与登记本店不一致时**两者都列** | 特商法表记 / gBizINFO |
| 日本参入年 | | 公司沿革 / 报道 |
| 直営店 | 实体店/展厅的有无与地址 | 官网 |
| 販売チャネル | 直营EC、量贩店、百货店的具体名称 | 官网 |
| 日本語公式サイト | | — |

> **「特定商取引法に基づく表記」是本产品最高价值的抓取目标**:日本法律强制在通信销售
> 页面公示法人名、所在地、代表者、电话。这是唯一一个"官网必然存在且必然真实"的页面。

#### 規制・安全性(6)

| 字段 | 说明 | 一次来源 |
|---|---|---|
| 技適(技術基準適合証明) | 无线设备的电波法强制认证 | 総務省 技適検索DB |
| PSE(電気用品安全法) | 电器/充电器的强制认证 | 経済産業省 |
| リコール・自主回収履歴 | 有则列日期与型号,无则写"检索范围内未发现" | 消費者庁 / 経産省 / 官网告知 |
| その他の法規制 | 航空法(无人机)、小売電気事業者登録 等 | 主管省厅 |
| 公的機関からの指摘 | 行政指导、措置命令 | 消費者庁 / NITE |
| データの保存先・越境移転 | **中国系品牌的核心疑虑点**;摄像头/语音数据存哪 | 隐私政策 |

#### 保証・アフターサービス(6)

保証期間 / 延長保証制度 / 修理体制 / 日本語サポート窓口 / サポート受付時間 /
消耗品・交換部品の入手性

> 保証期間**不能一刀切写"1年"**:同一品牌不同品类经常不同(如エレコム路由器1年、
> VR眼镜6个月),购买渠道也影响(官方店2年、认证店1年、平行进口无保)。必须写清条件。

#### 製品ラインと価格帯(4)

主力カテゴリ / 主な価格帯 / 主要製品ライン / **日本での主な競合**

> **「日本での主な競合」字段驱动站内互链**。`build.py:related_brands()` 会扫描该字段
> 文本,匹配站内已有品牌的 `name_en` / `name` / 括号内片假名,自动生成「関連ブランド」区块。
> 因此**新增品牌应优先从既有品牌的该字段中挑选**——加进来即自动接上双向链接。

### 3.3 事实对象

```jsonc
{
  "label": "保証期間",
  "value": "…",           // 含「未確認」或「【要確認】」则整行红字
  "source": "販売規約",    // 出处的页面名,不是域名
  "source_url": "https://…",
  "checked": "2026-08-28" // 确认日,页面上与出处并列显示
}
```

### 3.4 FAQ 对象

```jsonc
{
  "slug": "anker-country",        // [a-z0-9-]+,品牌内唯一
  "question": "…",                // 页面 h1,也是 <title>
  "source": {                     // 实投稿型必填
    "platform": "Yahoo!知恵袋",
    "url": "https://detail.chiebukuro.yahoo.co.jp/…",
    "original": "元帖标题原文ママ"
  },
  "answer": "300〜600字。结论先行。",
  "facts": [ /* 同 §3.3,渲染为「根拠となる事実」表 */ ],

  "topical": true,                // 时事型标记(无 source,改用 sources[])
  "sources": [{"name":"…","url":"…","published":"YYYY-MM-DD"}]
}
```

两种 FAQ:

| 类型 | 来源 | 标记 |
|---|---|---|
| **实投稿型** | Yahoo!知恵袋 / 価格.comクチコミ / 教えて!goo / OKWAVE / Quora日本版 / note / はてブ | 页首显示平台名 + 原帖标题 + 「元の投稿を見る」链接 |
| **时事型** | 制度变更、破产、召回等新闻 | `topical:true`,列表页加红色「最新の話題」徽章 |

**FAQ 问题必须来自真实投稿,不得凭空编写。** 这是产品可信度的基础:用户看到的是别人
真的问过的问题,而不是营销团队想象的问题。

FAQ 自动分类(`build.py:FAQ_CATEGORIES`,按关键词匹配问题文本):

`最新の話題`(topical标记) → `安全性・リコール` → `規制・法令` →
`保証・修理・サポート` → `他ブランドとの比較` → `ブランドの基本` → `その他の質問`

### 3.5 时间线对象(updates)

```jsonc
{
  "date": "2026-06-04",  // 严格 YYYY-MM-DD,必须是来源明记的日期
  "title": "…",          // 品牌内唯一,同时用于去重
  "body": "200〜400字",
  "impact": "对消费者的意义,1〜2句(可空)",
  "source": "…", "source_url": "https://…"
}
```

**日期规则是硬约束**:只接受来源中明确写出的日期。禁止由"星期几对得上"反推年份、
禁止用会期首日代替发表日、禁止任何形式的推定日期。宁可留空。

---

## 4. 页面规格

### 4.1 URL 结构

| 路径 | 页面 | 数量 |
|---|---|---|
| `/` | 品牌导航首页(卡片网格) | 1 |
| `/<brand>/` | 品牌定位页 | 16 |
| `/<brand>/<faq>/` | 问答页 | 300 |
| `/qa/` | 全站问题索引(按品牌分组) | 1 |
| `/status/` | 数据整备状况 | 1 |
| `/about/` | 关于本站・运营方针 | 1 |
| `/llms.txt` `/sitemap.xml` `/robots.txt` | AI/爬虫入口 | 3 |

FAQ 挂在品牌 slug 下(而非独立 `/qa/<slug>/`),使 URL 本身携带品牌语义,
且面包屑天然形成两级层级。

### 4.2 品牌定位页结构(自上而下)

```
パンくずリスト
h1「<品牌名>とはどんなブランドか」
meta 行:国别徽章 / 最終確認日
summary 摘要段
┌ 30秒でわかる ─────────────────┐
│ どこの国の会社か / 日本法人 /   │
│ 保証期間 / 価格帯 /             │
│ データ整備状況 N/32 項目確認済み │  ← 未确认数公开显示
└───────────────────────────┘
目次(锚点)
h2 最新の動向  → 时间线(纵向,红点节点)
h2 企業プロフィール      → 事实表
h2 日本での事業体制      → 事实表
h2 規制・安全性          → 事实表
h2 保証・アフターサービス → 事实表
h2 製品ラインと価格帯    → 事実表
h2 よくある質問  → 按分类分组的折叠区(<details>)
h2 関連ブランド  → 由「日本での主な競合」自动生成
h2 出典一覧      → 本页用到的一次来源去重列表
サンプル注記
```

事实表统一三列:**項目 / 内容 / 出典・確認日**。出处是可点击链接,确认日在其下方。

### 4.3 问答页结构

```
パンくずリスト(品牌 > 问题)
h1 = 问题原文
最終確認日
┌ 出典ボックス:Yahoo!知恵袋 / 原帖标题 / 「元の投稿を見る」┐
p.answer(::before 显示红色「結論」标签)  ← 首屏即答案
h2 根拠となる事実 → 事实表
h2 <品牌名>のブランド事実ページ / 他の質問
```

**首屏即答案**是本产品面向 AI 引用的关键设计:AI 抓取时第一段就是完整答案,
不需要理解页面结构。

---

## 5. 设计规范

遵循日本 Web 惯例:高信息密度、带框表格、明示更新日、面包屑、目录、折叠 FAQ。
**无 JavaScript,无外部依赖,CSS 内联在 `<style>` 中**(减少请求、保证抓取时样式完整)。

### 5.1 色板

| Token | 值 | 用途 |
|---|---|---|
| `--fg` | `#222` | 正文 |
| `--muted` | `#707070` | 次要信息、出处、确认日 |
| `--line` | `#dcdcdc` | 表格线、分隔线 |
| `--accent` | `#b71c1c` | 主色(深红)。页眉下边框、h2 左边框、时间线节点、日本徽章 |
| `--accent2` | `#1a237e` | 次色(藏青)。链接、海外徽章、品牌卡上边框 |
| `--bg` / `--bg2` | `#fff` / `#f7f7f5` | 背景 / 区块背景 |
| `.unverified` | `#d32f2f` | **未确认事实的红字** |
| 「結論」框 | 底 `#fdf6f6` 边 `#ecd5d5` | 问答页答案 |
| 「30秒でわかる」框 | 底 `#fdfbf3` 边 `#d7c9a7` 标签 `#8a6d1f` | 要点框 |

国别徽章:**日本 = 红(`--accent`)/ 海外 = 藏青(`--accent2`)**,首页卡片上边框同色。

### 5.2 排版

```
font-family: "Hiragino Kaku Gothic ProN","Hiragino Sans","Noto Sans JP",
             "Yu Gothic Medium","Meiryo",sans-serif   ← 全部系统字体,不加载 webfont
html font-size : 15px
line-height    : 1.85        ← 日文正文偏高行距
main max-width : 760px       ← 单栏,无侧边栏
h1: 1.45rem / 800   h2: 1.08rem / 700(灰底 + 5px 红色左边框)
断点: 560px(出典一覧 2栏→1栏,要点框标签换行)
```

---

## 6. AI 引用优化(说明书 §5.3)

| 措施 | 实现 |
|---|---|
| 品牌页 JSON-LD | `Article`(含 `about: Brand`、`dateModified`)+ `BreadcrumbList` + `FAQPage` |
| 问答页 JSON-LD | `FAQPage`(`Question.sameAs` 指向原帖 URL)+ `BreadcrumbList` |
| `llms.txt` | 自动生成。列出全部品牌页与问答页,每条带摘要与事实项目数 |
| `sitemap.xml` / `robots.txt` | 自动生成,可直接提交 GSC |
| 结论先行 | 问答页首段即完整答案 |
| 语义 URL | `/anker/anker-country/` 路径本身表达"Anker 的国籍问题" |
| 出处外链 | 每条事实带 `rel="noopener"` 外链到一次来源,提升可验证性 |

---

## 7. 数据质量原则(**产品的核心差异点**)

这一节不是技术细节,是产品定义。竞品能抄页面样式,抄不了这套约束。

### 7.1 未确认必须可见,不得隐藏

- 未在一次来源核实的字段,`value` 含「未確認」或「【要確認】」,**整行红字**。
- 「30秒でわかる」框固定显示「全32項目中 N項目が確認済み(未確認 M項目)」。
- `/status/` 页公开**每个品牌的确认率**和**跨品牌未确认最多的字段排行**。

> 反面做法:删掉查不到的字段,让页面看起来是满的。本产品明确禁止。

### 7.2 出处分级

| 用途 | 允许的来源 |
|---|---|
| **事实** | 品牌官方域名及其子域、政府机关(`*.go.jp`、gBizINFO)、海外法定披露(SEC)、主流媒体 |
| **问题** | Yahoo!知恵袋、価格.com、教えて!goo、OKWAVE、Quora日本版、note、はてブ |
| **均不可** | 个人博客、联盟营销站、内容农场 |

**论坛帖不能当事实依据**(可以当问题来源)。合并脚本会剔除这类引用。

### 7.3 数值与日期

- 金额/台数/份额/员工数:**来源明记才写**,否则留空或加「【要確認】」。
- 时间线日期:**来源明记的 `YYYY-MM-DD` 才写**。推定日期一律拒绝。
- 引用**实时行情页/库存页等易变页面**的数值属于无效出处——明天该 URL 就对不上了。

### 7.4 矛盾要并列,不要择一

两个可信来源冲突时(如特商法地址 vs 登记本店地址),**两者都写并标明各自出处**,
而不是挑一个看起来对的。

---

## 8. 内容生产管线

### 8.1 环境约束

本项目的调研环境**无法直接抓取网页**(egress proxy 拦截 WebFetch/curl)。
可行路径是 **WebSearch + `allowed_domains` 限定域名**,通过检索摘要获取官网内容。
因此:

- 优先攻击**摘要里会出正文**的页面(特商法表记、公司概要、PR TIMES 新闻稿)。
- 检索配额是 session 级的,**多品牌必须分批**,单品牌预算约 15〜20 次。
- 子代理必须被告知:**配额耗尽时立即落盘已有结果**,不要因失败丢失全部成果。

### 8.2 流程

```
subagent 调研(并行,分批)
   ↓ 写 JSON 到 staging/
staging/official/   → tools/merge_official.py    覆盖既有字段(红字转黑字)
staging/social/     → tools/merge_social.py      追加实投稿 FAQ
staging/monthly/    → tools/merge_monthly.py     追加时间线
staging/fresh/      → tools/merge_fresh.py       制度变更 → 时间线 + 时事 FAQ
staging/newbrand/   → tools/merge_new_brand.py   新建品牌 JSON
   ↓
python3 build.py    → docs/
   ↓ git push
GitHub Actions(pages.yml)→ gh-pages 分支 → 自动上线
```

**合并脚本是防线,不是搬运工。** 每个脚本在写入前校验,不通过的**剔除并打印原因**,
而不是静默接受。这样即使调研环节出错,错误也不会进入正式数据。

### 8.3 校验规则

| 校验项 | 规则 | 实现位置 |
|---|---|---|
| 事实出处主机 | 白名单(品牌官方域名及子域 / `*.go.jp` / SEC / 主流媒体) | 各 merge 脚本 |
| 问题出处主机 | QA 平台白名单 | `merge_social` `merge_new_brand` |
| 时间线日期 | `^20\d{2}-\d{2}-\d{2}$` | `merge_monthly` `merge_new_brand` |
| **法人番号** | **国税庁检查用数字校验:`9 - (Σ Pn×Qn) mod 9`,Pn 为后12位从右第n位,n 奇数 Qn=1、偶数 Qn=2。不一致则拒收** | `merge_official` `merge_new_brand` |
| 新品牌区块名 | 必须与5个规定名称完全一致,否则丢弃 | `merge_new_brand` |
| 新品牌标准字段 | 调研结果缺失的字段**不删除**,以「未確認」补位 | `merge_new_brand` |
| 字段名异体 | `法人番号`→`法人番号(13桁)`、`技適`→`技適(技術基準適合証明)` 等归一 | `merge_new_brand` |
| 去重 | slug / 问题文 / 原帖 URL / 时间线标题 | 全部脚本 |

> 法人番号校验位是这套体系里**唯一能纯机器验证真伪**的字段,因此它是检验调研可信度的
> 探针:一批结果里若有校验位不符的号码,说明该批次存在编造,应整体复查。

---

## 9. AI 引用监测

`monitor/monitor.py` + `monitor/queries.json`。

- 固定日语 query 集,按主题分组:`brand_country`(哪国公司)、`trust_safety`(安全疑虑)、
  `regulation`(法规)等。query 措辞**贴近真实用户投稿的说法**。
- 每日向 ChatGPT / Perplexity / Gemini 提问,记录是否引用本站及被引用片段。
- 结果落 `monitor/results/YYYY-MM-DD.jsonl`,输出当日引用率。
- 未设置对应 API key 的 provider 自动跳过。

---

## 10. 部署

- **无依赖**:`python3 build.py` 读 `data/` 生成 `docs/`,标准库以外零依赖。
- **构建确定性**:同样输入必产出同样输出(已验证重复构建无 diff)。
- GitHub Actions `pages.yml`:push 到目标分支 → 构建 → 强推 `gh-pages` → Pages 自动更新。
- 子路径支持:`config.json` 的 `base_url` 路径部分自动作为站内链接前缀
  (`https://x.github.io/PS-test` → 所有内链加 `/PS-test`)。换正式域名只改这一处。

---

## 11. 当前状态与差距(2026-08-28)

| 指标 | 值 |
|---|---|
| 品牌 | 16 |
| 事实字段 | 521 项(**366 项带出处已核实 = 70%**) |
| Q&A | 300 条 |
| 时间线 | 146 条 |
| 生成页面 | 320 页 |
| 法人番号已确认 | **13 / 16** |

**核实率必须分开看**:最早的 10 个品牌 **84%**(276/329),新加的 6 个品牌 34〜59%。
整体 70% 是两批的加权平均。

### 已知差距

| # | 差距 | 卡点 |
|---|---|---|
| 1 | UGREEN 34% / ECOVACS 38% / GoPro 47% | 「特定商取引法に基づく表記」页面存在但正文不进检索摘要 |
| 2 | 新6品牌 FAQ 各10条(老品牌24条) | 调研预算 |
| 3 | Dyson 2026年7月、Roborock 2026年8月时间线空白 | 两轮独立检索均无日期可坐实的日本市场事件 |
| 4 | Insta360 / iRobot / ECOVACS 法人番号未确认 | 登记簿中查无对应法人名或地址不符 |
| 5 | 技適的逐型号取得状况几乎全品牌未确认 | 総務省 DB 不支持机器批量查询 |
| 6 | 全站未经日语母语审校 | 上线前必须完成(说明书 §6.1) |

> 差距 3、4 是**主动留空**的结果,不是遗漏。填入推定值会破坏 §7 的产品定义。

---

## 12. 后续路线

**M1 补完度**
1. 新 6 品牌核实率拉到 70%+,重点攻特商法表记与 IR 页
2. 新品牌 FAQ 扩到 24 条/品牌
3. 剩余 3 个法人番号人工确认

**M2 规模**
4. 品牌数扩到 30+,继续沿「日本での主な競合」字段扩散(保证互链密度)
5. 品类横向对比页(如「ロボット掃除機ブランド一覧」),复用既有事实字段

**M3 自动化**
6. 技適 / 召回的 headless 批量抓取管线(说明书既定项)
7. 法人番号国税庁 API 自动查询
8. 事实的**再确认到期机制**:`checked` 超过 N 个月自动进复查队列

**M4 商业化**
9. 联盟转链(说明书规定 M4 之后启用)
